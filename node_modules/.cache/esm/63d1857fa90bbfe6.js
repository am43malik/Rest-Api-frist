let valid,Custumerrorhandler;_a90‍.x([["default",()=>_a90‍.o]]);_a90‍.w("joi",[["valid",["valid"],function(v){valid=v}]]);_a90‍.w("../error/CustumErrorhandler",[["default",["Custumerrorhandler"],function(v){Custumerrorhandler=v}]]);

const errorHandller = (error, req, res, next) => {
  let statusCode = 405;
  let data = {
    message: "Internal server Error ",
    orignalError: error.message,
  };

  if (error instanceof valid) {
    data = {
      message: error.message,
    };
  }

if(error instanceof Custumerrorhandler){
  res.status(error.status).json({
    message:error.message
  })
}





  

  return res.status(statusCode).json(data);
};

_a90‍.d(errorHandller);
